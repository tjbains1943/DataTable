import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#ADD8E6",
    },
    secondary: {
      main: "#ADD8E6",
    },
    error: {
      main: "#B20000",
    },
    background: {
      default: "#fffeea",
    },
  },
});

export default theme;
